{{template "base/head" .}}
<div class="dashboard feeds">
	<div class="ui container">
		<p>this is mirror</p>
		<p>{{.Name}}</p>

		{{range .Mirrors}}

		ID:{{.ID}}
		<br/>
		repoId:{{.Repo.ID}}
		<br/>
		repoName:{{.Repo.Name}}
		<br/>

		{{end}}
	</div>
</div>
{{template "base/footer" .}}
